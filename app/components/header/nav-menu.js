import React from 'react'
import Button from '../index'

const NavMenu = ({ btns }) => {
  console.log({btns})
  return (
    <div>
      <Button label='test' />
    </div>
  )
}

export default NavMenu
